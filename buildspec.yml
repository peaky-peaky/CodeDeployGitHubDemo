version: 0.2

phases:
  install:
    commands:
      - yum install curl
  pre_build:
    commands:
      - yum update
  build:
    commands:
      - echo "Hello, World! (phase=build)"
      - ls -al /home/vagrant
